package com.company;

public class Main {

    public static void main(String[] args) {

        //Strings data type
        String myString = "This is a string";
        System.out.println("myString is equals to " + myString);
        myString = myString + ", this is more";
        System.out.println("myString is equal to "+ myString);
        //Strings are immutable. We can not changed the string instead we crete new strings and old one discarded
    }
}
